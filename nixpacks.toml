[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmd = "cd next-frontend && npm install"

[phases.build]
cmd = "cd next-frontend && npx next build"

[start]
cmd = "cd next-frontend && NODE_ENV=production npm start"
